package ord.example.homework.second.office;

public class Secretary {
    public void stopThisShit(Boss boss, Manager manager, Security security) {
        System.out.println(boss.name + " не волнуйтесь, " + manager.name + " все успеет. "
                + security.name + " подождите!");
    }
}
